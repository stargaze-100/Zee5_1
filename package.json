{
  "name": "Zee5_1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "node-gyp": "^7.1.0",
    "nuxt": "^2.14.5"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "gh-pages": "^3.1.0",
    "push-dir": "^0.4.1"
  }
}
